// Collision Event with obj_broom

// Change the sprite to the hit sprite
sprite_index = spr_nun_hit;

// Optional: Start a timer to revert back to the original sprite after a short period
alarm[0] = 30; // 30 frames (adjust as needed)
